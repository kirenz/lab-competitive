---
title: "Resources"
---

## Prerequisites

To start this lab, you'll need:

- **SQL**: MySQL, MySQL Workbench and a database called `db_ecommerce`
- **Python**: Anaconda, Visual Studio Code and Visual Studio Code Extensions
- **Working environment**: A folder on your machine called `lab-competitive` and an environment file with your MySQL password

## SQL

### MySQL

- MySQL (select "No thanks, just start my download")
  - [Windows](https://dev.mysql.com/downloads/installer/)
  - [Mac](https://dev.mysql.com/downloads/mysql/)

### MySQL Workbench

- [MySQL Workbench](https://dev.mysql.com/downloads/workbench/)

### Create Database

Use MySQL Workbench to create a new database called `db_ecommerce`

```sql
DROP DATABASE IF EXISTS `db_ecommerce`;
CREATE DATABASE `db_ecommerce`; 
USE `db_ecommerce`;
```

## Python

### Anconda

- [Anaconda](https://www.anaconda.com/download)

### Visual Studio Code

- [Visual Studio Code](https://code.visualstudio.com/)

### Visual Studio Code Extensions

- [Visual Studio Code Extensions](https://vscode.dev/profile/github/94d1b9a0184dec88655f9622c87b866d)


### Anaconda Environment

Install the Anaconda Environment `env-mr-pip.yml`

- [💾 Anaconda Environment](https://github.com/kirenz/environments)

## Working environment

### Folder

Create a new folder called `lab-competitive`

### Environment file

You need to use an env file to store environment variables () file with your MySQL database password to access MySQL.

env file to store environment variables. This file contains key-value pairs in the format KEY=VALUE , with each variable on a new line. We can use libraries like python-dotenv to load the variables from the file into our Python script automatically.


#### MacOS

1. [Download the `env` file](/.env) in your Downloads folder (the file is hidden)
2. Open Finder and navigate to the Downloads folder 

<!--
3. Press the “Command” + “Shift” + “.” (period) keys at the same time. The hidden files will show up as translucent in the folder.
-->
3. Move the file in your folder `lab-competitive`.
4. Open the file and replace `YOUR-PASSWORD` with your MySQL password.
5. Save the changes.

<!--
7. If you want to obscure the files again, press the same “Command” + “Shift” + “.” (period) combination.
-->

#### Windows 10

1. [Download the `env` file](/.env) in your Downloads folder (the file is hidden)
2. Open File Explorer from the taskbar. 

<!--
3. Select View > Options > Change folder and search options.
4. Select the View tab and, in Advanced settings, select Show hidden files, folders, and drives and OK.
-->
3. Move the file in your folder `lab-competitive`.
4. Open the file and replace `YOUR-PASSWORD` with your MySQL password.
5. Save the changes.


#### Windows 11

1. [Download the `env` file](/.env) in your Downloads folder (the file is hidden)
2. Open File Explorer from the taskbar. 
<!--
3. Select View > Show > Hidden items.
-->
3. Move the file in your folder `lab-competitive`.
4. Open the file and replace `YOUR-PASSWORD` with your MySQL password.
5. Save the changes.